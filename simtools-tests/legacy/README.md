# Legacy test resources

This directory contains historical test resources that predate the current
generated-resource structure.

Legacy versions are preserved for reference and compatibility only. They are
not generated, validated, linted, or otherwise supported by the current CI
workflows. New generated test resources must be added as version directories
directly below `simtools-tests/`, not in this directory.
